# كيف أعدّل موقع «ناب الصحراء» — دليل سريع

دليل مرجعي بسيط، ارجع له كل ما نسيت الخطوات.

---

## 📂 الخطوة 1: افتح الأداة
- روح لمجلد الموقع اللي فكيت ضغطه على جهازك (مجلد `site`)
- ابحث عن ملف اسمه **`editor.html`**
- اضغط عليه مرتين — بيفتح في المتصفح (كروم مثلًا)

## 👀 الخطوة 2: شوف المحتوى الحالي
بيظهر لك أعلى الصفحة **تبويبات**:

`دليل الأفاعي` · `مغامراتي وصيدي` · `مكتبة الصور` · `مكتبة الفيديوهات` · `مكتبة المقالات`

اضغط على أي تبويب لتشوف كل العناصر الموجودة فيه حاليًا كبطاقات، كل بطاقة فيها حقول قابلة للتعديل.

## ✏️ الخطوة 3: عدّل — مثال عملي (إضافة صورة)
1. اضغط تبويب **"مكتبة الصور"**
2. انزل لآخر الصفحة، اضغط زر **"+ إضافة مكتبة الصور جديد"**
3. بيظهر لك عنصر فارغ جديد، فيه حقلين:
   - **مسار الصورة**: اكتب مثلًا `images/صورتي.jpg`
   - **الوصف**: اكتب الوصف بثلاث خانات (عربي AR / عبري HE / إنجليزي EN)
4. (لاحقًا، لازم تحط ملف الصورة الفعلي داخل مجلد `images` بنفس الاسم اللي كتبته)

**لتعديل عنصر موجود:** فقط امسح النص القديم من أي حقل واكتب الجديد — يتحدث فورًا في الخلفية.

**لحذف عنصر:** اضغط الزر الأحمر **"حذف هذا العنصر"** أعلى البطاقة.

نفس الطريقة بالضبط تنطبق على باقي التبويبات (المغامرات، الفيديوهات، المقالات، دليل الأفاعي).

## 💾 الخطوة 4: احفظ التعديلات (الأهم!)
انزل لأسفل الصفحة تمامًا، واضغط الزر:

**⬇ تنزيل data.js المحدّث**

بينزلّك ملف اسمه `data.js`.

## 📁 الخطوة 5: ضع الملف الجديد مكان القديم
1. روح لمجلد `js` داخل مجلد الموقع
2. احذف ملف `data.js` القديم (أو خليه واسمه شي ثاني احتياط)
3. انقل ملف `data.js` الجديد اللي نزّلته لنفس المجلد `js`

## ✅ الخطوة 6: شوف النتيجة
افتح `index.html` (أو أعد تحميل الصفحة لو كانت مفتوحة أصلًا بـ F5) — التعديل بيظهر مباشرة.

---

## ⚠️ نصيحة مهمة
لو أغلقت `editor.html` بدون ما تضغط زر التنزيل، تعديلاتك تضيع (لأنها ما تُحفظ تلقائيًا).
**دايمًا اضغط التنزيل قبل ما تسكر الصفحة.**

## 🔁 لو غلطت وتبي ترجع للأصل
داخل `editor.html`، اضغط زر **"استعادة المحتوى الأصلي"** أسفل الصفحة — بيرجعك لآخر نسخة كانت محمّلة بالأداة (وليس بالضرورة النسخة المنشورة إن كنت صدّرت وحفظت من قبل).

---

*هذا الملف جزء من موقع ناب الصحراء — احتفظ فيه مع بقية ملفات الموقع.*
